{
    "version": "0.2.0",
    "configurations": [
        {
            "cwd": "${workspaceFolder}/server/src/",
            "name": "Python: Huidige bestand",
            "type": "python",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "args": ["-d","models"]
        }
    ]
}